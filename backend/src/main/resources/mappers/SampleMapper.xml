<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.plands.backend.mapper.SampleMapper">
    <select id="findAll" resultType="com.plands.backend.domain.Sample">
        SELECT user_sq, user_name, user_email FROM USERS_TB;
    </select>
</mapper>
